import { VietnameseMonthPipe } from './vietnamese-month.pipe';

describe('VietnameseMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new VietnameseMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
